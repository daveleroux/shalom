<li>
  <%= link_to user.username, edit_user_path(user) %>
  <% if current_user.admin? %>
      | <%= link_to "delete", user, :method => :delete, :confirm => "You sure?", :title => "Delete #{user.username}" %>
  <% end %>
</li>
