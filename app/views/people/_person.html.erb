<tr>
  <td><input type="checkbox" value="<%= person.id %>" checked="yes" /></td>
  <td><%= link_to person.name, party_path(person) %></td>
   <td><%= link_to "delete", party_path(person), :method => :delete, :confirm => "You sure?", :title => "Delete #{person.name}" %></td>
</tr>