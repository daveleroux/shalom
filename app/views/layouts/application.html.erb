<!DOCTYPE html>
<html>
<head>
  <title>Shalom - StudentY Student Administration</title>
    <%= csrf_meta_tags %>
  <%= render 'layouts/stylesheets' %>

</head>

<body>
    <div class="container">
      <%= render 'layouts/header' %>

      <section class="round">
        <%= yield %>
      </section>
    </div>
  </body>

</html>
